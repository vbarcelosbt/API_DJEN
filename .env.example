# Copie este arquivo para .env e preencha os valores do robo.
# O .env NAO deve ser versionado.
# Os criterios de pesquisa no DJEN ficam no painel de controle do config.py.

NOME_CLIENTE=adicione aqui o nome do cliente
NOME_ROBO=API_DJEN

# Opcional: destinatarios dos e-mails de inicio/fim, separados por virgula.
# Sem esta variavel, valem os enderecos definidos em config.py.
LISTA_EMAIL_TI=
