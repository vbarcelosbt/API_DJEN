# Fonte canonica das dependencias: pyproject.toml (gerenciado com uv).
# Este arquivo existe para quem usa pip/venv:  pip install -r requirements.txt
# Ao adicionar uma dependencia, atualize os dois arquivos.

beautifulsoup4>=4.13.4
google-api-python-client>=2.179.0
openpyxl>=3.1.5
pandas>=2.3.2
python-dotenv>=1.1.1
pytz>=2025.2
selenium>=4.35.0

# Bibliotecas internas (clonar na raiz do projeto, nao instalar por pip):
#   git clone https://github.com/bluetapesd/classes_complementares
#   git clone https://github.com/bluetapesd/classe_objeto_pagina
#   git clone https://github.com/bluetapesd/Logger
